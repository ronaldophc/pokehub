services:
    app:
        build:
            context: .
            dockerfile: Dockerfile.coolify
            args:
                user: pokehub
                uid: 1000
                VITE_REVERB_APP_KEY: ${VITE_REVERB_APP_KEY}
                VITE_REVERB_HOST: ${VITE_REVERB_HOST}
                VITE_REVERB_PORT: ${VITE_REVERB_PORT:-443}
                VITE_REVERB_SCHEME: ${VITE_REVERB_SCHEME:-https}
        image: pokehub_app:latest
        container_name: pokehub_app
        restart: unless-stopped
        working_dir: /var/www
        networks:
            - pokehub-network
        depends_on:
            - db
        labels:
            - traefik.enable=true
            - traefik.http.routers.pokehub.rule=Host(`${APP_DOMAIN}`)
            - traefik.http.routers.pokehub.entrypoints=https
            - traefik.http.routers.pokehub.tls.certresolver=letsencrypt
            - traefik.http.services.pokehub.loadbalancer.server.port=80

    reverb:
        image: pokehub_app:latest
        container_name: pokehub_reverb
        restart: unless-stopped
        working_dir: /var/www
        command: php artisan reverb:start --host=0.0.0.0 --port=8080
        networks:
            - pokehub-network
        depends_on:
            - app

    queue:
        image: pokehub_app:latest
        container_name: pokehub_queue
        restart: unless-stopped
        working_dir: /var/www
        command: php artisan queue:work --sleep=3 --tries=3 --max-time=3600
        networks:
            - pokehub-network
        depends_on:
            - app
            - db

    db:
        image: mysql:8.0
        container_name: pokehub_db
        restart: unless-stopped
        environment:
            MYSQL_DATABASE: ${DB_DATABASE}
            MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
            MYSQL_USER: ${DB_USERNAME}
            MYSQL_PASSWORD: ${DB_PASSWORD}
        volumes:
            - dbdata:/var/lib/mysql
        networks:
            - pokehub-network

networks:
    pokehub-network:
        driver: bridge

volumes:
    dbdata:
        driver: local
